# Core
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic>=2.0,<3.0
pydantic-settings>=2.0,<3.0
python-dotenv==1.0.1

# Database
sqlalchemy[asyncio]==2.0.36
aiosqlite==0.20.0
alembic==1.14.0
asyncpg==0.30.0

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# passlib 1.7.4 breaks on bcrypt 5.x; bcrypt 4.1+ drops __about__ (noisy warnings). Use 4.0.x.
bcrypt>=4.0.1,<4.1
python-multipart>=0.0.7

# LLM
httpx>=0.27,<1.0

# RAG — vector store
qdrant-client>=1.9,<2.0
# RAG — Phase 1: cross-encoder reranker (lazy-loaded; ~90 MB on first use; CPU-only / Apple M2)
sentence-transformers>=3.0,<4.0
# RAG — Phase 5: AST code chunker (tree-sitter; ~1 MB; fast CPU parsing)
tree-sitter>=0.21,<0.25
tree-sitter-python>=0.25
tree-sitter-javascript>=0.25

# SSH
fabric>=3.2,<4.0
paramiko>=3.5,<4.0

# Document Generation
reportlab>=4.1,<5.0
python-docx>=1.0,<2.0
python-pptx>=0.6,<1.0
openpyxl>=3.1,<4.0
Pillow>=10.0,<11.0

# Image Generation (optional — SDXL Turbo local provider)
# Uncomment the following to enable local SDXL Turbo image generation:
# diffusers[torch]>=0.27,<1.0
# transformers>=4.38,<5.0
# accelerate>=0.27,<1.0

# PDF extraction
pypdf>=4.0,<5.0

# Security
cryptography>=42.0,<44.0

# Utilities
structlog>=24.0,<25.0

# Browser automation
playwright>=1.44,<2.0

# Testing
pytest>=8.0
pytest-asyncio>=0.24
pytest-cov>=5.0
pytest-benchmark>=4.0
pytest-json-report>=1.5
factory-boy>=3.3
httpx>=0.27,<1.0

# MCP SDK — testing server + client protocol
mcp>=1.3,<2.0
